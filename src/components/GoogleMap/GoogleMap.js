import React, { Component, createRef } from 'react';
import './index.css';

const GoogleMap = props => {
    return (
        <div>
            <div id="map"></div>
        </div>
    )
}

export default GoogleMap;
